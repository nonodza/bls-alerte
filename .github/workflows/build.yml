name: BLS ULTRA FIXED FINAL
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3

      - name: Create ULTRA App
        shell: python
        run: |
          import os, pathlib
          os.system("rm -rf app build.gradle settings.gradle gradle.properties .gradle gradle")
          pathlib.Path("app/src/main/java/com/bls/alerte").mkdir(parents=True, exist_ok=True)
          open("settings.gradle","w").write("include ':app'")
          open("gradle.properties","w").write("android.useAndroidX=true")
          open("build.gradle","w").write("buildscript{repositories{google();mavenCentral()} dependencies{classpath 'com.android.tools.build:gradle:8.5.2'}} allprojects{repositories{google();mavenCentral()}}")
          open("app/build.gradle","w").write("apply plugin: 'com.android.application' android{namespace 'com.bls.alerte';compileSdk 34;defaultConfig{applicationId 'com.bls.alerte';minSdk 23;targetSdk 34;versionCode 60;versionName 'ULTRA-FIXED'}}")
          pathlib.Path("app/src/main").mkdir(par
